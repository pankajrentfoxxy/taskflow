# Secret used to sign session cookies. CHANGE THIS IN PRODUCTION.
AUTH_SECRET=change-me-to-a-long-random-string

# Optional: secret required to call /api/cron/sla-check from an external scheduler.
# (The app also runs the SLA sweep automatically on activity, at most once per minute.)
CRON_SECRET=
